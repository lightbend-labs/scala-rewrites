version: ~> 1.0 # needed for imports
import: scala/scala-dev:travis/default.yml

language: scala
env: ADOPTOPENJDK=8
scala:
  - 2.12
  - 2.12.next
script: sbt test

notifications:
  email:
    on_success: never
